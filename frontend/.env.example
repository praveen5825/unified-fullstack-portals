VITE_API_BASE_URL=http://localhost:8000/api
# Adjust this to match your existing accounts app's login endpoint.
# Must return JSON with an access token field (access_token or access).
VITE_LOGIN_ENDPOINT=/accounts/login/
